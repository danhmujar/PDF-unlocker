---
plan: "05-02"
name: "Integrity & Policy Hardening"
wave: 2
depends_on: ["05-01"]
requirements: ["REQ-5.1", "REQ-5.2"]
must_haves:
  truths:
    - "Application boot fails if any core asset (JS/CSS) has been tampered with."
    - "CSP v3 prevents unauthorized script execution and unapproved network calls."
  artifacts:
    - path: "index.html"
      provides: "Hardened security baseline for the application"
  key_links:
    - from: "index.html"
      to: "ui/app.js"
      via: "SRI integrity attribute"
---

# 05-02: Integrity & Policy Hardening

## Tasks

<task>
<files>
- "scripts/generateSri.js"
- "scripts/verifySri.js"
</files>
<action>
Create `scripts/generateSri.js` to generate SHA-384 base64 hashes for all local assets.
Create `scripts/verifySri.js` to validate that `index.html` contains the correct `integrity` attributes for all referenced local assets, as required by `VALIDATION.md` Protocol A-5.1.
</action>
<verify>
<automated>node scripts/generateSri.js && node scripts/verifySri.js</automated>
</verify>
<done>
- [ ] Implement `scripts/generateSri.js`.
- [ ] Implement `scripts/verifySri.js`.
- [ ] List all required assets (app.js, styles.css, pdfService.js, batchService.js, auditService.js, pdfWorker.js).
</done>
</task>

<task>
<files>
- "index.html"
</files>
<action>
Update `index.html` with SRI hashes generated in the previous task. Add `integrity` and `crossorigin="anonymous"` to all script and link tags. Harden the CSP to include `object-src 'none'` and ensure `worker-src 'self' blob:;` is preserved.
</action>
<verify>
<automated>node scripts/verifySri.js</automated>
</verify>
<done>
- [ ] Apply SRI hashes to all `<script>` and `<link>` tags in `index.html`.
- [ ] Update CSP `<meta>` tag with stricter policies (preserving `worker-src`).
- [ ] Verify app still loads correctly in the browser.
</done>
</task>

<task>
<files>
- "tests/e2e/security.spec.js"
</files>
<action>
Create `tests/e2e/security.spec.js`. Implement tests to verify:
1. Scripts with mismatched SRI are blocked.
2. Unauthorized network requests are blocked by CSP.
3. `eval()` is blocked by CSP.
</action>
<verify>
<automated>npx playwright test tests/e2e/security.spec.js --reporter=line</automated>
</verify>
<done>
- [ ] Implement SRI failure test.
- [ ] Implement CSP network block test.
- [ ] Implement CSP eval block test.
</done>
</task>

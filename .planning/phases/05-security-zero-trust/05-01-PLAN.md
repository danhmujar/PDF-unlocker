---
plan: "05-01"
name: "Advanced Security Logic & Persistence"
wave: 1
depends_on: []
requirements: ["REQ-5.3", "REQ-5.4"]
must_haves:
  truths:
    - "Application logs all processing events to a persistent local audit trail."
    - "Every decrypted PDF is cryptographically hashed with SHA-256 in the worker."
  artifacts:
    - path: "services/auditService.js"
      provides: "IndexedDB audit logging singleton"
    - path: "services/pdfWorker.js"
      provides: "SHA-256 hashing in the background thread"
  key_links:
    - from: "pdfWorker.js"
      to: "auditService.js"
      via: "worker message passing and service call"
---

# 05-01: Advanced Security Logic & Persistence

## Tasks

<task>
<files>
- "services/auditService.js"
- "tests/auditService.test.js"
</files>
<action>
Create `services/auditService.js`. Implement a singleton `AuditService` that uses IndexedDB to store records in a `logs` store within `PDF_UNLOCKER_DB` (v1). Define `logEvent(type, details)` where `type` is one of 'INFO', 'SUCCESS', 'ERROR' and `details` is a JSON object. Follow the IIFE pattern to encapsulate private state and expose the service to `window.auditService`.
Create `tests/auditService.test.js` to verify IndexedDB initialization and log persistence.
</action>
<verify>
<automated>npx vitest run tests/auditService.test.js</automated>
</verify>
<done>
- [ ] Initialize IndexedDB `PDF_UNLOCKER_DB` (v1) with `logs` store.
- [ ] Implement `logEvent(type, details)` method.
- [ ] Implement `getLogs()` method for later retrieval.
- [ ] Export as `window.auditService` using IIFE pattern.
- [ ] Implement unit tests in `tests/auditService.test.js`.
</done>
</task>

<task>
<files>
- "services/pdfWorker.js"
- "tests/pdfWorker.test.js"
</files>
<action>
Update `services/pdfWorker.js` to use `self.crypto.subtle.digest('SHA-256', outputBuffer)` on the decrypted PDF buffer. Return the hex-encoded string as a `hash` property in the message sent back to the main thread.
Update `tests/pdfWorker.test.js` to verify that the worker response includes a valid 64-character SHA-256 hex string.
</action>
<verify>
<automated>npx vitest run tests/pdfWorker.test.js</automated>
</verify>
<done>
- [ ] Implement `bufferToHex` helper function in `pdfWorker.js`.
- [ ] Calculate SHA-256 hash using Web Crypto API.
- [ ] Include `hash` in the success response message.
- [ ] Update `tests/pdfWorker.test.js` to assert on hash property.
</done>
</task>

<task>
<files>
- "services/pdfService.js"
- "index.html"
- "tests/pdfService.test.js"
</files>
<action>
1. Include `services/auditService.js` in `index.html`.
2. Update `services/pdfService.js` to call `window.auditService.logEvent` with file processing details (name, status, hash).
3. Ensure all error paths in `pdfService.js` log an 'ERROR' event.
4. Update `tests/pdfService.test.js` to mock/verify `auditService` calls.
</action>
<verify>
<automated>npx vitest run tests/pdfService.test.js</automated>
</verify>
<done>
- [ ] Add script tag for `services/auditService.js` to `index.html`.
- [ ] Integrate `auditService.logEvent` in `pdfService.js` success handler.
- [ ] Integrate `auditService.logEvent` in `pdfService.js` error handlers.
- [ ] Update `tests/pdfService.test.js` to verify logging integration.
</done>
</task>

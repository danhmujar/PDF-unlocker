---
plan: "05-03"
name: "Security UX & Integration"
wave: 3
depends_on: ["05-02"]
requirements: ["REQ-5.3", "REQ-5.4"]
must_haves:
  truths:
    - "User can view the SHA-256 hash of a processed file on its UI card."
    - "User can access a local-only audit trail showing history of processed files."
  artifacts:
    - path: "ui/app.js"
      provides: "Audit viewer logic and hash rendering"
    - path: "ui/styles.css"
      provides: "Styles for the Audit Log and security indicators"
  key_links:
    - from: "ui/app.js"
      to: "services/auditService.js"
      via: "event listener for 'View Log' button"
---

# 05-03: Security UX & Integration

## Tasks

<task>
<files>
- "ui/app.js"
- "ui/styles.css"
- "tests/e2e/securityUx.spec.js"
</files>
<action>
Update the `FileCard` rendering in `ui/app.js`. Add a section at the bottom of the card to display the SHA-256 hash returned from the worker. Implement a "Verified" badge with a tooltip. Update `ui/styles.css` with monospaced styles for the hash and a green glow for the badge.
Begin implementing `tests/e2e/securityUx.spec.js` to verify the hash is displayed correctly.
</action>
<verify>
<automated>npx playwright test tests/e2e/securityUx.spec.js --reporter=line</automated>
</verify>
<done>
- [ ] Add `hash` display to `FileCard` template in `ui/app.js`.
- [ ] Implement hover tooltip for the "Verified" status.
- [ ] Add CSS for hash typography and verified badge in `ui/styles.css`.
- [ ] Implement initial hash visibility test in `tests/e2e/securityUx.spec.js`.
</done>
</task>

<task>
<files>
- "index.html"
- "ui/app.js"
- "ui/styles.css"
- "tests/e2e/securityUx.spec.js"
</files>
<action>
Implement the Security Audit Trail modal. Add a button to the About modal to trigger it. The modal should fetch logs from `auditService.getLogs()` and display them in a responsive table. Columns: Timestamp, Action, Filename, Result, Hash.
Update `tests/e2e/securityUx.spec.js` to verify the audit log contents.
</action>
<verify>
<automated>npx playwright test tests/e2e/securityUx.spec.js --reporter=line</automated>
</verify>
<done>
- [ ] Add HTML structure for the Audit Modal in `index.html`.
- [ ] Implement `openAuditLog` logic in `ui/app.js`.
- [ ] Add table styling and "Copy Hash" functionality in `ui/styles.css`.
- [ ] Update `tests/e2e/securityUx.spec.js` to verify audit log persistence and display.
</done>
</task>

<task>
<files>
- "tests/e2e/securityUx.spec.js"
</files>
<action>
Finalize `tests/e2e/securityUx.spec.js`. Test the full security flow:
1. Process a file.
2. Verify the hash appears on the card.
3. Open the Audit Log.
4. Confirm the entry exists and matches the card's hash.
5. Verify "Copy Hash" interaction works.
</action>
<verify>
<automated>npx playwright test tests/e2e/securityUx.spec.js --reporter=line</automated>
</verify>
<done>
- [ ] Complete E2E test for full security flow.
- [ ] Verify "Copy Hash" interaction.
- [ ] Ensure all UX elements respond as expected.
</done>
</task>

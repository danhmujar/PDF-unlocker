---
phase: 02-ux-polish-batch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [services/pdfService.js, ui/app.js]
autonomous: true
requirements: [REQ-2.1]
must_haves:
  truths:
    - "Multiple PDFs process concurrently rather than sequentially"
    - "Individual file status updates are reflected in the service layer"
  artifacts:
    - path: "services/pdfService.js"
      provides: "WorkerPool class for parallel execution"
    - path: "ui/app.js"
      provides: "Queue management utilizing the WorkerPool"
  key_links:
    - from: "ui/app.js"
      to: "services/pdfService.js"
      via: "WorkerPool instance"
---

<objective>
Implement a Web Worker Pool to enable concurrent PDF processing and individual status tracking.

Purpose: Improve performance for batch uploads and allow granular feedback for each file.
Output: WorkerPool class and updated UI queue manager.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@services/pdfService.js
@ui/app.js
@services/pdfWorker.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement WorkerPool class</name>
  <files>services/pdfService.js, tests/pdfService.test.js</files>
  <behavior>
    - Should initialize N workers based on navigator.hardwareConcurrency.
    - Should distribute files to idle workers.
    - Should emit events/callbacks for individual file progress, success, and error.
    - Should handle worker errors and re-initialize if necessary.
  </behavior>
  <action>
    - Refactor `pdfService.js` to include a `WorkerPool` class.
    - Use `Promise.withResolvers()` (or manual constructor) to track task completion.
    - Implement a task queue within the pool.
    - Ensure workers are reused rather than recreated for every file.
  </action>
  <verify>
    <automated>npm test tests/pdfService.test.js</automated>
  </verify>
  <done>WorkerPool successfully distributes tasks across multiple workers and passes unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate WorkerPool into UI Queue Manager</name>
  <files>ui/app.js</files>
  <action>
    - Replace the sequential `nextInQueue` logic in `ui/app.js` with `pdfService.WorkerPool.enqueue()`.
    - Update `processQueue` to initialize the pool if it doesn't exist.
    - Map WorkerPool events to UI updates (currently to `updateStatus`, but prepare for Bento Grid).
    - Maintain the `isQueueRunning` flag correctly for the entire batch.
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -Command "Select-String -Path ui/app.js -Pattern 'WorkerPool'"</automated>
  </verify>
  <done>UI processes multiple files using the WorkerPool and maintains correct batch state.</done>
</task>

</tasks>

<verification>
- [ ] Multiple workers are visible in browser DevTools (Sources > Threads).
- [ ] Batch of 5 small PDFs processes significantly faster than sequential.
- [ ] UI reflects 'processing' state until the last file in the pool is finished.
</verification>

<success_criteria>
Worker Pool architecture is active, enabling parallel processing of up to 20 files with individual status reporting hooks.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ux-polish-batch/02-01-SUMMARY.md`
</output>

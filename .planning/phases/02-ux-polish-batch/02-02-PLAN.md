---
phase: 02-ux-polish-batch
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [index.html, ui/styles.css, ui/app.js, tests/e2e/batch.spec.js]
autonomous: true
requirements: [REQ-2.3]
must_haves:
  truths:
    - "Drop Zone content animates into a Bento Grid when files are added"
    - "Each file in a batch has its own visual card with status icons"
    - "Drop zone remains active/accessible when grid is displayed for adding more files"
  artifacts:
    - path: "ui/styles.css"
      provides: "Bento Grid and File Card styling"
    - path: "ui/app.js"
      provides: "Dynamic card rendering and View Transition logic"
    - path: "tests/e2e/batch.spec.js"
      provides: "E2E tests for batch processing and grid UI"
  key_links:
    - from: "ui/app.js"
      to: ".bento-grid container"
      via: "DOM manipulation in renderBentoGrid"
---

<objective>
Implement a Bento Grid layout for batch file processing with smooth visual transitions and E2E test coverage.

Purpose: Provide enterprise-grade feedback for multiple files and modernize the UI.
Output: Bento Grid UI component, View Transitions integration, and E2E test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ux-polish-batch/02-01-SUMMARY.md
@ui/styles.css
@ui/app.js
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create E2E Batch Test Scaffold</name>
  <files>tests/e2e/batch.spec.js</files>
  <action>
    - Create `tests/e2e/batch.spec.js` using Playwright.
    - Add test cases for:
        - Drag and drop multiple files.
        - Verifying the appearance of the Bento Grid.
        - Verifying that individual cards are created.
        - Checking that the drop zone remains active/receptive after the grid appears.
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -Command "Test-Path tests/e2e/batch.spec.js"</automated>
  </verify>
  <done>E2E test scaffold is ready for verification of the Bento Grid implementation.</done>
</task>

<task type="auto">
  <name>Task 1: Define Bento Grid and File Card CSS</name>
  <files>ui/styles.css</files>
  <action>
    - Add `.bento-grid` styles using `display: grid` with `auto-fill` and `minmax`.
    - Create `.file-card` styles following the Aurora/Glass aesthetic (glassmorphism, subtle borders).
    - Add status modifiers for cards: `.pending`, `.processing`, `.success`, `.error`.
    - Ensure grid items can span multiple columns/rows for small batches (Bento style).
    - Respect `prefers-reduced-motion` for all transitions.
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -Command "Select-String -Path ui/styles.css -Pattern '\.bento-grid'"</automated>
  </verify>
  <done>CSS classes for Bento Grid and file cards are defined and responsive.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Dynamic Grid Rendering with View Transitions</name>
  <files>ui/app.js, index.html</files>
  <action>
    - Add a container for the grid in `index.html`.
    - Create a `renderBentoGrid(files)` function in `ui/app.js`.
    - Wrap the UI swap (Awaiting -> Grid) in `document.startViewTransition()` if available.
    - Generate file cards dynamically, mapping each card to a WorkerPool task ID.
    - Update individual card status (icon/text) as pool events arrive.
    - **Warning Implementation**: Ensure the drop zone remains active/accessible when the Bento Grid is displayed so users can append more files to the batch.
    - Reduce logic churn in `ui/app.js` by focusing on DOM updates.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/batch.spec.js</automated>
  </verify>
  <done>UI smoothly transitions to a grid of cards upon file drop, with individual status updates and active drop zone.</done>
</task>

</tasks>

<verification>
- [ ] Dropping 3 files results in 3 distinct cards in a grid.
- [ ] View transition is visible (content "morphs" or fades smoothly).
- [ ] Cards update from 'pending' to 'processing' to 'success' independently.
- [ ] Dropping more files while the grid is active adds them to the queue.
</verification>

<success_criteria>
Bento Grid UI is fully functional, providing a modern, responsive layout for batch processing with fluid animations and verified E2E coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ux-polish-batch/02-02-SUMMARY.md`
</output>

---
phase: 02-ux-polish-batch
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified: [ui/styles.css, ui/app.js, index.html, tests/theme.test.js]
autonomous: true
requirements: [REQ-2.4]
must_haves:
  truths:
    - "User can choose between 4 themes: Light, Dark, Aurora, Glass"
    - "Aurora theme displays animated mesh gradients"
    - "Glass theme displays high-translucency glassmorphism effects"
  artifacts:
    - path: "ui/styles.css"
      provides: "Semantic variable maps for 4 themes"
    - path: "ui/app.js"
      provides: "4-way theme toggle logic"
    - path: "tests/theme.test.js"
      provides: "Unit tests for theme switching and persistence"
  key_links:
    - from: "index.html"
      to: "ui/styles.css"
      via: "data-theme attribute on <html>"
---

<objective>
Implement 4 distinct themes (Light, Dark, Aurora, Glass), an expanded theme selector, and theme unit tests.

Purpose: Provide personalized visual experiences and leverage modern CSS effects.
Output: Updated theme variable system, 4-way selector UI, and theme test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ui/styles.css
@ui/app.js
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Theme Unit Tests</name>
  <files>tests/theme.test.js</files>
  <action>
    - Create `tests/theme.test.js` using Vitest and JSDOM.
    - Add tests to:
        - Verify that changing the theme update the `data-theme` attribute on `<html>`.
        - Verify that the theme selection persists in `localStorage`.
        - Check that switching through all 4 themes works as expected.
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -Command "Test-Path tests/theme.test.js"</automated>
  </verify>
  <done>Unit test scaffold for theme logic is ready.</done>
</task>

<task type="auto">
  <name>Task 1: Map CSS Variables for 4 Themes</name>
  <files>ui/styles.css</files>
  <action>
    - Refactor `:root` to use Semantic variables (e.g., `--bg-main`, `--text-primary`).
    - Define 4 theme blocks: `[data-theme="light"]`, `[data-theme="dark"]`, `[data-theme="aurora"]`, `[data-theme="glass"]`.
    - Aurora: Light-based with mesh gradient background.
    - Glass: Dark-based with `backdrop-filter: blur(12px)` and low-opacity backgrounds for cards.
    - Ensure all existing components use the new semantic variables.
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -Command "Select-String -Path ui/styles.css -Pattern '\[data-theme=\"aurora\"\]'"</automated>
  </verify>
  <done>All 4 themes are defined with a consistent variable mapping system.</done>
</task>

<task type="auto">
  <name>Task 2: Update Theme Toggle UI and Logic</name>
  <files>index.html, ui/app.js</files>
  <action>
    - Update the theme toggle in `index.html` to a multi-click button that cycles 4 themes.
    - Update `ui/app.js` to cycle through `['light', 'dark', 'aurora', 'glass']`.
    - Update `localStorage` to persist the chosen theme name.
    - Ensure the SVG icons in the toggle reflect the current theme state.
  </action>
  <verify>
    <automated>npm test tests/theme.test.js</automated>
  </verify>
  <done>Theme selector allows switching between 4 distinct visual styles and persists across reloads, verified by unit tests.</done>
</task>

</tasks>

<verification>
- [ ] Switching to 'Aurora' shows the animated gradient.
- [ ] Switching to 'Glass' shows transparent, blurred card backgrounds.
- [ ] Theme selection persists after page refresh.
</verification>

<success_criteria>
The application supports 4 high-quality themes with a robust variable system and automated unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ux-polish-batch/02-04-SUMMARY.md`
</output>

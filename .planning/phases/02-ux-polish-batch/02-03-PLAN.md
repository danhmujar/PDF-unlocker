---
phase: 02-ux-polish-batch
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: [ui/app.js, ui/styles.css, index.html, services/batchService.js]
autonomous: true
requirements: [REQ-2.2]
must_haves:
  truths:
    - "User is prompted to choose between ZIP or Individual downloads after processing"
    - "Individual downloads are triggered with a 300-500ms delay to avoid browser blocking"
    - "ZIP batch size is capped at 150MB with a user warning if exceeded"
  artifacts:
    - path: "services/batchService.js"
      provides: "Business logic for ZIP orchestration and throttled downloads"
    - path: "ui/app.js"
      provides: "Post-processing UI event handling"
    - path: "index.html"
      provides: "Post-processing selector UI elements"
  key_links:
    - from: "ui/app.js"
      to: "services/batchService.js"
      via: "batchService calls for ZIP/Individual downloads"
    - from: "services/batchService.js"
      to: "JSZip"
      via: "new JSZip() call"
---

<objective>
Implement flexible download options for batch processing with security-aware execution and 3-tier architecture.

Purpose: Prevent browser download blocking and give users control over output format.
Output: Post-processing selector UI and batch service layer.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ux-polish-batch/02-02-SUMMARY.md
@ui/app.js
@ui/styles.css
@assets/vendor/jszip.min.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Batch Service Layer</name>
  <files>services/batchService.js</files>
  <action>
    - Create `services/batchService.js` to encapsulate business logic for batch operations.
    - Implement `batchService.downloadIndividually(files)` with a throttled loop (400ms delay).
    - Implement `batchService.downloadAsZip(files)` using `JSZip`, including the 150MB limit check.
    - Export these functions for use by the UI layer.
    - Ensure zero DOM dependency in this file (Strict 3-Tier Architecture).
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -Command "Test-Path services/batchService.js"</automated>
  </verify>
  <done>Business logic for batch downloads is moved to a dedicated service layer.</done>
</task>

<task type="auto">
  <name>Task 2: Create Post-Processing Selector UI</name>
  <files>index.html, ui/styles.css</files>
  <action>
    - Add a "Batch Complete" overlay or action bar in `index.html` (initially hidden).
    - Style two primary action buttons: "Download as ZIP" and "Download Individually".
    - Add a status summary showing total files successfully unlocked.
    - Implement a warning message for batches exceeding 150MB (ZIP option disabled or warned).
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -Command "Select-String -Path index.html -Pattern 'Download as ZIP'"</automated>
  </verify>
  <done>UI for download selection is created and styled.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Batch Service into UI</name>
  <files>ui/app.js</files>
  <action>
    - Refactor `ui/app.js` to import and use `services/batchService.js`.
    - Delegate download triggering to the service layer.
    - Update individual card status or show progress during ZIP generation.
    - Ensure `URL.revokeObjectURL` is handled correctly (either in service or via callbacks).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/batch.spec.js</automated>
  </verify>
  <done>UI layer is decoupled from batch processing logic, adhering to the 3-tier architecture.</done>
</task>

</tasks>

<verification>
- [ ] Finishing a batch of 5 files shows the "Download Options" selector.
- [ ] Clicking "Individually" triggers 5 downloads with visible gaps.
- [ ] Clicking "ZIP" produces a single .zip file.
- [ ] ZIP option is visually disabled if total size > 150MB.
</verification>

<success_criteria>
Users can choose their preferred download method for batches, with business logic cleanly separated from the UI layer.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ux-polish-batch/02-03-SUMMARY.md`
</output>

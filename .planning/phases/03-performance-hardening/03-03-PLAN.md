---
phase: 03-performance-hardening
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified: [ui/app.js, services/batchService.js]
autonomous: true
requirements: [REQ-3.4]
must_haves:
  truths:
    - "A warning toast or sub-status message appears when total batch size exceeds 1GB"
    - "Progress messages reflect the scale of processing for large files"
  artifacts:
    - path: "ui/app.js"
      provides: "Heavy load detection logic"
  key_links:
    - from: "ui/app.js"
      to: "batchService.js"
      via: "size checking in queueFiles"
---

<objective>
Implement "Heavy Load" warnings and predictive progress feedback for large processing tasks. Ensure users are informed when they are pushing browser limits.

Purpose: Prevent user frustration during long-running tasks.
Output: Informative UI for high-load scenarios.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-performance-hardening/03-RESEARCH.md
@.planning/phases/03-performance-hardening/03-CONTEXT.md
@ui/app.js
@services/batchService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Heavy Load UI Warning</name>
  <files>ui/app.js</files>
  <action>
    Add a check in queueFiles (ui/app.js) to calculate the total size of the current batch.
    
    Specifics:
    - If total size > 1024MB (1GB), update the sub-status text with a "Heavy Load Warning".
    - Message: "Heavy Load: Processing 1GB+ may take several minutes. Ensure your browser tab remains active."
    - Style: Add a specific CSS class or use 'updateStatus' with an 'info' or 'warning' state if applicable.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/batch.spec.js</automated>
  </verify>
  <done>User is warned when dropping a batch larger than 1GB.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Predictive Progress Feedback</name>
  <files>ui/app.js</files>
  <action>
    Update the onStatus callback in processQueue (ui/app.js) to provide scale-aware feedback.
    
    Implementation:
    - If file size > 250MB, change status messages to include "Large File Optimization active...".
    - Provide more granular steps in the sub-status (e.g., "Step 1/3: Mounting virtual filesystem", "Step 2/3: Unlocking core...", "Step 3/3: Finalizing memory...").
    - Rationale: Large files stay in "Unlocking" for longer; detailed steps reduce perceived wait time.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/pwa_ui.spec.js</automated>
  </verify>
  <done>Progress UI shows more detail for large files.</done>
</task>

<task type="auto">
  <name>Task 3: Refine ZIP Size Limits</name>
  <files>services/batchService.js</files>
  <action>
    Review and potentially increase MAX_ZIP_SIZE_BYTES in batchService.js.
    
    Changes:
    - Research browser jszip limits (typically ~2GB but browser memory is the limit).
    - Increase threshold to 1GB to match the new file support level.
    - Keep the warning in the UI (app.js) for batches near this limit.
  </action>
  <verify>
    <automated>npm test tests/batchService.test.js</automated>
  </verify>
  <done>ZIP size limit is synchronized with the new engine capabilities.</done>
</task>

</tasks>

<verification>
Drop a 1.1GB batch. Verify that the warning message appears. Observe the progress sub-status during processing of a large file.
</verification>

<success_criteria>
- Warnings appear correctly for large batches.
- Progress messages are informative for long tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-performance-hardening/03-03-SUMMARY.md`
</output>

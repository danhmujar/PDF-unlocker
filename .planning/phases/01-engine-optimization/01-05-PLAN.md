---
phase: 01-engine-optimization
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [services/pdfService.js, tests/pdfService.test.js]
autonomous: false
requirements: [TST-2]
must_haves:
  truths:
    - "User interface remains responsive during processing"
    - "User is notified when PDF is ready"
  artifacts:
    - path: "services/pdfService.js"
      provides: "Main thread Proxy for Worker communication"
    - path: "tests/pdfService.test.js"
      provides: "Main thread Proxy verification"
  key_links:
    - from: "services/pdfService.js"
      to: "services/pdfWorker.js"
      via: "new Worker('services/pdfWorker.js')"
      pattern: "new Worker"
---

<objective>
Refactor the main thread pdfService to act as a proxy to the new Web Worker.

Purpose: Connect the UI to the newly isolated worker engine without blocking the main thread.
Output: Refactored pdfService.js and updated tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/01-RESEARCH.md
@services/pdfService.js
@tests/pdfService.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor services/pdfService.js to Proxy</name>
  <files>services/pdfService.js</files>
  <action>
    Refactor services/pdfService.js to act as a lightweight Proxy/Client.
    - Instantiate the Worker and handle message routing.
    - Maintain isProcessing state on the main thread based on Worker messages.
    - Proxy processFile calls to the Worker using postMessage.
    - Map Worker 'ready', 'status', 'success', and 'error' messages back to the existing onStatus callback (or a new onReady callback if needed).
  </action>
  <verify>
    <automated>grep "postMessage" services/pdfService.js</automated>
  </verify>
  <done>pdfService.js successfully proxies requests to the Worker and handles callbacks.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor tests/pdfService.test.js</name>
  <files>tests/pdfService.test.js</files>
  <action>
    Update the existing pdfService.test.js to handle the new worker-based architecture.
    - Mock the Web Worker environment in Vitest.
    - Ensure tests still pass by simulating Worker message events.
    - Verify that postMessage is called with the expected arguments, including Transferables.
  </action>
  <verify>
    <automated>npm test tests/pdfService.test.js</automated>
  </verify>
  <done>Existing tests refactored to support the worker-based architecture.</done>
</task>

</tasks>

<verification>
- Run unit tests for pdfService.
- Manually verify that uploading a PDF still works and successfully unlocks the file.
</verification>

<success_criteria>
- Existing UI callbacks (onStatus) are correctly triggered by Worker events.
- Service is covered by unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-optimization/01-05-SUMMARY.md`
</output>
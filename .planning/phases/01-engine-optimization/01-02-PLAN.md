---
phase: 01-engine-optimization
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [services/pdfWorker.js]
autonomous: false
requirements: [FR-2, FR-5, SEC-2, UI-3]
must_haves:
  truths:
    - "PDF engine initializes noticeably faster"
    - "Users are protected from tampered engine binaries"
    - "Users see clear error messages if the engine fails to load"
  artifacts:
    - path: "services/pdfWorker.js"
      provides: "Streaming WASM initialization logic"
  key_links:
    - from: "services/pdfWorker.js"
      to: "WebAssembly.instantiateStreaming"
      via: "fetch(url, { integrity })"
      pattern: "instantiateStreaming"
---

<objective>
Optimize engine bootstrap by implementing Streaming WASM instantiation and enforcing Subresource Integrity (SRI) for security.

Purpose: Reduce time-to-first-processing and ensure secure binary loading.
Output: Performant and secure WASM initialization in the Worker context.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/01-RESEARCH.md
@services/pdfWorker.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement instantiateStreaming and SRI</name>
  <files>services/pdfWorker.js</files>
  <action>
    Refactor initWasm within services/pdfWorker.js to use WebAssembly.instantiateStreaming combined with SRI validation.
    - Implement the Emscripten instantiateWasm hook to override the default binary fetcher.
    - Attach the SRI hash (sha384-9ESKDLiqwqZ9ln5RdWhoE5TM/zLYG2UoW/AMa0KeND/fhDO5ZJsRH6FTJ3Dera+p) to the fetch call.
    - Ensure the response is passed directly to instantiateStreaming for optimal performance.
  </action>
  <verify>
    <automated>grep "instantiateStreaming" services/pdfWorker.js</automated>
  </verify>
  <done>Streaming WASM instantiation with SRI implemented in the Worker.</done>
</task>

<task type="auto">
  <name>Task 2: Robust Error Handling (CSP/Network)</name>
  <files>services/pdfWorker.js</files>
  <action>
    Enhance error handling for WASM initialization in the Worker context.
    - Capture Network errors during fetch.
    - Capture CompileError or EvalError that signal CSP blocks (e.g., missing 'wasm-unsafe-eval').
    - Send descriptive error messages back to the main thread (type: 'error') so the UI can report correctly.
  </action>
  <verify>
    <automated>npm test tests/pdfWorker.test.js</automated>
  </verify>
  <done>Worker robustly handles and reports initialization failures.</done>
</task>

</tasks>

<verification>
- Verify in browser DevTools Network tab that the WASM request uses streaming (no preliminary ArrayBuffer fetch if possible, though Emscripten's hook might still show up).
- Manually test by changing the SRI hash to an invalid value and confirming the engine fails to load with an integrity error.
- Check the console for any CSP-related warnings during initialization.
</verification>

<success_criteria>
- WASM initialization uses `instantiateStreaming`.
- SRI validation prevents tampered binaries from loading.
- Errors are accurately propagated to the main thread.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-optimization/01-02-SUMMARY.md`
</output>

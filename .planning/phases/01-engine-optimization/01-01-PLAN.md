---
phase: 01-engine-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [services/pdfWorker.js, tests/pdfWorker.test.js]
autonomous: true
requirements: [FR-1, SEC-3, TST-1]
must_haves:
  truths:
    - "Heavy PDF processing does not freeze the UI"
    - "Users can process sensitive PDFs securely without disk writes"
  artifacts:
    - path: "services/pdfWorker.js"
      provides: "WASM runtime & processing logic"
    - path: "tests/pdfWorker.test.js"
      provides: "Worker logic verification"
  key_links:
    - from: "tests/pdfWorker.test.js"
      to: "services/pdfWorker.js"
      via: "import Worker"
      pattern: "pdfWorker"
---

<objective>
Migrate the QPDF WASM engine to a Web Worker and ensure entirely in-memory processing.

Purpose: Offload resource-intensive PDF processing to prevent main-thread blocking and secure data handling.
Output: Working PDF processing engine running in an isolated Worker thread using MEMFS.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/01-RESEARCH.md
</context>

<tasks>

<task type="manual">
  <name>Task 0: Initial Memory Search and Skill Orchestration</name>
  <files></files>
  <action>
    - Run memory_search({ query: "engine optimization", type: "decision" }) to check for prior context.
    - Invoke @antigravity-skill-orchestrator to load necessary skills based on current project path and stack.
    - Ensure all dependencies are understood before proceeding to task 1.
  </action>
  <verify>
    <automated>echo "Memory search and orchestration complete"</automated>
  </verify>
  <done>Memory and skills initialized successfully.</done>
</task>

<task type="auto">
  <name>Task 1: Create services/pdfWorker.js</name>
  <files>services/pdfWorker.js</files>
  <action>
    Extract the core WASM initialization and processing logic into a new services/pdfWorker.js file.
    - Implement a structured message protocol: { type: 'init' }, { type: 'process', file: ArrayBuffer, name: string }.
    - Worker should respond with: { type: 'ready' }, { type: 'status', state: string, main: string, sub: string }, { type: 'success', blob: ArrayBuffer, name: string }, { type: 'error', main: string, sub: string }.
    - Use Transferable Objects for zero-copy data transfer of ArrayBuffers.
    - Import scripts from unpkg (qpdf.js) using importScripts. (Note: importScripts does not support SRI; security is focused on the WASM binary SRI in Plan 02).
  </action>
  <verify>
    <automated>ls services/pdfWorker.js</automated>
  </verify>
  <done>Worker logic implemented with message listeners and Transferable support.</done>
</task>

<task type="auto">
  <name>Task 2: Implement MEMFS In-Memory Processing (SEC-3)</name>
  <files>services/pdfWorker.js</files>
  <action>
    Ensure all document processing happens entirely in-memory using Emscripten's virtual MEMFS within the worker.
    - Mount ArrayBuffer directly into MEMFS before passing to the QPDF WASM instance.
    - Read the output directly from MEMFS into a new ArrayBuffer.
    - Clean up (unlink) files from MEMFS immediately after processing to free memory.
    - Ensure no data is ever written to persistent storage (like IDBFS).
  </action>
  <verify>
    <automated>grep "MEMFS" services/pdfWorker.js</automated>
  </verify>
  <done>Worker utilizes MEMFS for all processing and cleans up memory correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/pdfWorker.test.js</name>
  <files>tests/pdfWorker.test.js</files>
  <action>
    Create a unit test for the Worker logic in isolation.
    - Use Vitest to mock the Worker environment or handle the file as a standard JS module for logic testing.
    - Verify that the Worker correctly processes a mock ArrayBuffer, interacts with MEMFS properly, and emits the appropriate success/error messages.
  </action>
  <verify>
    <automated>npm test tests/pdfWorker.test.js</automated>
  </verify>
  <done>Worker logic and MEMFS interactions verified by unit tests.</done>
</task>

</tasks>

<verification>
- Run unit tests for pdfWorker.
- Verify in browser DevTools that the 'qpdf.wasm' is now loaded by a Worker, not the main thread.
</verification>

<success_criteria>
- PDF processing is offloaded to the worker thread.
- Zero-copy (Transferable Objects) is used for data transfer.
- Files are processed securely in MEMFS.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-optimization/01-01-SUMMARY.md`
</output>
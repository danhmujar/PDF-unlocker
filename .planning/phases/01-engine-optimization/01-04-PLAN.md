---
phase: 01-engine-optimization
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-05"]
files_modified: [tests/perf_test.js, index.html]
autonomous: true
requirements: [TST-3, SEC-1]
must_haves:
  truths:
    - "Performance comparison between V1 and V2 engine models is documented"
    - "System successfully unlocks a PDF using the new worker-based engine"
  artifacts:
    - path: "tests/perf_test.js"
      provides: "Benchmark comparing main-thread vs worker-thread processing"
  key_links:
    - from: "tests/perf_test.js"
      to: "services/pdfService.js"
      via: "performance.now() measurement"
      pattern: "performance\\.now"
---

<objective>
Verify the performance benefits of the worker-based engine and ensure final system integration.

Purpose: Quantify the improvement and ensure no regression in processing reliability.
Output: Benchmarking results and verified integrated system.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/01-RESEARCH.md
@services/pdfService.js
@services/pdfWorker.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/perf_test.js</name>
  <files>tests/perf_test.js</files>
  <action>
    Create a performance benchmark script that measures the time taken to process a PDF in the new worker-based engine.
    - Measure processing time for a representative PDF file.
    - Log results to console.
    - Compare with main-thread processing to quantify the improvement and meet TST-3 requirements.
  </action>
  <verify>
    <automated>node tests/perf_test.js</automated>
  </verify>
  <done>Performance benchmark script created and running.</done>
</task>

<task type="auto">
  <name>Task 2: Final Integration & CSP Verification</name>
  <files>index.html</files>
  <action>
    Perform final verification of the integrated system.
    - Ensure all CSP directives in index.html are compatible with Workers and WASM.
    - Confirm 'worker-src' and 'wasm-unsafe-eval' are correctly configured if needed.
    - Verify final end-to-end functionality: user can upload a PDF, it processes in worker, and triggers download.
  </action>
  <verify>
    <automated>grep -E "worker-src|wasm-unsafe-eval" index.html</automated>
  </verify>
  <done>Full system integrated and CSP compliant.</done>
</task>

<task type="manual">
  <name>Task 3: Persistence and Version Control</name>
  <files></files>
  <action>
    - Run memory_write to save learned patterns and decisions from Phase 1.
    - Format and execute a git commit command using Conventional Commits.
  </action>
  <verify>
    <automated>git log -1</automated>
  </verify>
  <done>Changes committed and memories saved.</done>
</task>

</tasks>

<verification>
- Run the benchmark script and check the results.
- Verify that large files (>50MB) are processed without blocking the UI main thread (UI remains interactive, e.g., about modal can be opened/closed during processing).
- Final check of the console for any CSP errors or Worker-related warnings.
</verification>

<success_criteria>
- Performance benchmark script is available and functional.
- The UI main thread remains unblocked (60fps) throughout PDF processing.
- Full E2E functionality is verified.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-optimization/01-04-SUMMARY.md`
</output>
